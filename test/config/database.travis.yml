sqlite: &sqlite
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000
  database: ":memory:"

development:
  <<: *sqlite

test:
  <<: *sqlite

staging:
  <<: *sqlite

production:
  <<: *sqlite
